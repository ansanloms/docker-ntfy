# 外部からアクセスする URL
# デフォルト: http://localhost:${NTFY_PORT:-2586}
#
# LAN 内のスマホから受け取る場合は PC のローカル IP を指定:
#   NTFY_BASE_URL=http://192.168.1.100:2586
#
# Tailscale 経由の場合はノード登録後に確認した Tailscale IP を指定:
#   docker compose exec tailscale tailscale ip -4
#   NTFY_BASE_URL=http://100.x.x.x
#   ※ Tailscale はコンテナ内部のポート 80 に直接届くのでポート不要
#
# 両方使う場合は LAN IP を設定しておけばよい (Tailscale 側はポートが違うだけで動く)
# NTFY_BASE_URL=http://192.168.1.100:2586

# ホスト側ポート (デフォルト: 2586)
# NTFY_PORT=2586

# メッセージのキャッシュ保持期間 (デフォルト: 1h)
# NTFY_CACHE_DURATION=1h

# Tailscale 認証キー
# https://login.tailscale.com/admin/settings/keys で発行
#
# おすすめ設定:
#   Reusable: オン (コンテナ再作成時にキーを使い回せる)
#   Ephemeral: オン (コンテナ停止時にマシン一覧から自動削除)
#   有効期限: 短め (1日など。登録完了後は不要になるため)
#
# 登録完了後は ts-state/ に状態が保存されるため
# この値を消しても動き続ける
# TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxx

# restart policy (no | always | on-failure | unless-stopped)
# RESTART_POLICY=unless-stopped

# vim:set ft=sh:
